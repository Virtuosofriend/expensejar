import { Enum } from "@/helpers/enum";

export const CONFIG = Enum({
    default_language:   "en",
    starting_year:  2021,
    starting_month: 10,
    month_expense_target: 2000,
});

export default CONFIG;